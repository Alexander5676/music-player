# Music Player (Vite + React)

Run locally:
```bash
npm install
npm run dev
```

This project is a minimal starter for a React music player using HTML5 Audio, Tailwind, Framer Motion and lucide icons.
Placeholder WAV tracks are included in `public/music` and covers in `public/covers`.
